# Local Supabase API URL.
VITE_SUPABASE_URL=http://127.0.0.1:54321

# For local CLI projects, use the anon key reported by:
# npx supabase@2.88.1 status
# Keep the variable name stable even though the local value is an anon key.
VITE_SUPABASE_PUBLISHABLE_KEY=replace-with-local-anon-key
